import styled from "styled-components";

export const Container = styled.div`
  background: var(--lightColor);
  height: 3.43rem;
  width: 100%;
  padding: 0.5rem 3rem;
  position: absolute;
  top: 0;
  left: 0;
  well: {
    box-Shadow: 0px 30px 0px #000000,
  },
`;
